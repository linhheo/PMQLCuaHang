@model IEnumerable<BOHOPUB2HAND.Models.Nhacungcap>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Nhà cung cấp<</h2>

<p>
    @Html.ActionLink("Thêm nhà cung cấp mới", "Create");
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NhaCungCapID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenNhaCungCap)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DiaChỉ)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoDienThoai)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.NhaCungCapID)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TenNhaCungCap)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.DiaChỉ)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.SoDienThoai)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Email)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.NhaCungCapID }) |
        @Html.ActionLink("Details", "Details", new { id = item.NhaCungCapID }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.NhaCungCapID })
    </td>
</tr>
}

</table>
